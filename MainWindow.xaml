<Window x:Class="DshAntigravityLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="DeepSeek Harness &amp; Antigravity Proxy Launcher" Height="750" Width="1200"
        Background="#0F172A" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Dark modern tab style -->
        <Style x:Key="TabButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0,0,0,3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                    <Setter Property="Background" Value="#1E293B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TabButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource TabButtonStyle}">
            <Setter Property="Foreground" Value="#38BDF8"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#38BDF8"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="Foreground" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#334155"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Row 0: Header bar with tabs -->
            <RowDefinition Height="Auto"/>
            <!-- Row 1: Main Content Area -->
            <RowDefinition Height="*"/>
            <!-- Row 2: Status Banner (Footer) -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0" x:Name="HeaderPanel" Background="#0B0F19" BorderBrush="#1E293B" BorderThickness="0,0,0,1" Padding="12,8,12,0" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title & Brand -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0,20,8">
                    <Border Width="10" Height="10" CornerRadius="5" Background="#10B981" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="DSH &amp; Proxy" Foreground="#F8FAFC" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Navigation Tabs -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button x:Name="BtnTabDsh" Content="⚡ Deepseek Harness" Style="{StaticResource TabButtonActiveStyle}" Click="BtnTabDsh_Click"/>
                    <Button x:Name="BtnTabProxy" Content="🚀 Antigravity Proxy" Style="{StaticResource TabButtonStyle}" Click="BtnTabProxy_Click"/>
                    <Button x:Name="BtnTabCodexProxy" Content="🤖 Codex Proxy" Style="{StaticResource TabButtonStyle}" Click="BtnTabCodexProxy_Click"/>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,8">
                    <Button x:Name="BtnReload" Content="🔄 Reload" Style="{StaticResource ActionButtonStyle}" Click="BtnReload_Click" ToolTip="Reload current webview"/>
                    <Button x:Name="BtnStopServices" Content="🛑 Stop Services" Style="{StaticResource ActionButtonStyle}" Click="BtnStopServices_Click" ToolTip="Stop DSH and Antigravity Proxy services"/>
                    <Button x:Name="BtnRestartServices" Content="⚡ Restart Services" Style="{StaticResource ActionButtonStyle}" Click="BtnRestartServices_Click" ToolTip="Restart background processes"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Webview Containers (Main Body) -->
        <Grid Grid.Row="1" Background="#0F172A">
            <!-- Webview 1: Deepseek Harness (Default) -->
            <wv2:WebView2 x:Name="WebDsh" Visibility="Visible"/>

            <!-- Webview 2: Antigravity Proxy -->
            <wv2:WebView2 x:Name="WebProxy" Visibility="Collapsed"/>

            <!-- Webview 3: Codex Proxy -->
            <wv2:WebView2 x:Name="WebCodexProxy" Visibility="Collapsed"/>
        </Grid>

        <!-- Status Banner (Footer) -->
        <Border Grid.Row="2" x:Name="StatusBanner" Background="#1E1B4B" BorderBrush="#3730A3" BorderThickness="0,1,0,0" Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="Initializing setup &amp; installing proxy packages..." Foreground="#E0E7FF" FontSize="12" FontWeight="Medium"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtPorts" Text="DSH: 3080 | Proxy: 8080 | Codex: 8081" Foreground="#A5B4FC" FontSize="11" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <Button x:Name="BtnToggleHeaderFooter" Content="👁️ Show Header" Style="{StaticResource ActionButtonStyle}" Padding="8,3" FontSize="11" Click="BtnToggleHeader_Click" ToolTip="Show/Hide top header bar"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
